# Development-only shell environment example. The app does not auto-load this file.
# Never use this path for normal installations; use Keychain through the UI or `init`.
SMS_BRIDGE_ALLOW_ENV_TOKEN=1
TELEGRAM_BOT_TOKEN=
